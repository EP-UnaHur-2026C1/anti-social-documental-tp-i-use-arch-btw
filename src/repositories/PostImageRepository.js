const { PostImage } = require('../models');

class PostImageRepository {
    async create(data) {}

    async findById(id) {}

    async deleteById(id) {}
}

module.exports = new PostImageRepository();
